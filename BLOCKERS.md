# Blockers Log

Tracked here so work never stops waiting on an answer.

| Date | Blocker | Status |
|------|---------|--------|
| 2026-08-08 | No licensed 3D anatomy `.glb` asset available. Using free CC0 placeholder geometry (primitive shapes labeled with organ names) per instructions. | Resolved by design - placeholder geometry in use |
| 2026-08-08 | Deploy token lacks `workflow` scope, so GitHub Actions workflow files cannot be pushed. Switched to classic GitHub Pages serving the `docs/` directory on `master` (auto-rebuilds on every push). | Resolved - static export synced to `docs/`, source path `/docs` |
